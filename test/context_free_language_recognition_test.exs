defmodule ContextFreeLanguageRecognitionTest do
  use ExUnit.Case
  doctest ContextFreeLanguageRecognition

  test "greets the world" do
    assert ContextFreeLanguageRecognition.hello() == :world
  end
end
